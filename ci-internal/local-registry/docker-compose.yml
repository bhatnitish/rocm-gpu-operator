---

version: "3.9"
services:
  registry:
    restart: always
    image: registry.test.pensando.io:5000/registry:2
    ports:
      - "5000:5000"
    environment:
      REGISTRY_STORAGE_FILESYSTEM_ROOTDIRECTORY: /data
    volumes:
      - ./data:/data

---
version: 2.0
meta:


image:
  bind_dir: "/gpu-operator"
  work_dir: "/gpu-operator"
  

e2e-targets:
  gpu-operator-pytest-v1.0.0:
    commands: [
        "/bin/sh",
        "-c",
        "cat /warmd.json && cd /gpu-operator/tests/pytests && \
         /gpu-operator/ci-internal/run_sanity.sh --type compat --testbed /warmd.json \
         --amdgpu-driver deviceconfig --image-manifest image-manifest/release_v1.0.0_internal_images.yaml"
    ]
    owners: ["email:srivatsa.sangli@amd.com"]
    area:
    sub-area:
    resources:
    - generic:
      - type: K8-GPU-Node
        name: AMD-GPU-MI210
        vendor: Pensando
        count: 1

  gpu-operator-pytest-v1.1.0:
    commands: [
        "/bin/sh",
        "-c",
        "cat /warmd.json && cd /gpu-operator/tests/pytests && \
         /gpu-operator/ci-internal/run_sanity.sh --type compat --testbed /warmd.json \
         --amdgpu-driver deviceconfig --image-manifest image-manifest/release_v1.1.0_internal_images.yaml"
    ]
    owners: ["email:srivatsa.sangli@amd.com"]
    area:
    sub-area:
    resources:
    - generic:
      - type: K8-GPU-Node
        name: AMD-GPU-MI210
        vendor: Pensando
        count: 1

  gpu-operator-pytest-v1.2.0:
    commands: [
        "/bin/sh",
        "-c",
        "cat /warmd.json && cd /gpu-operator/tests/pytests && \
         /gpu-operator/ci-internal/run_sanity.sh --type compat --testbed /warmd.json \
         --amdgpu-driver deviceconfig --image-manifest image-manifest/release_v1.2.0_internal_images.yaml"
    ]
    owners: ["email:srivatsa.sangli@amd.com"]
    area:
    sub-area:
    resources:
    - generic:
      - type: K8-GPU-Node
        name: AMD-GPU-MI210
        vendor: Pensando
        count: 1

  gpu-operator-pytest-v1.2.1:
    commands: [
        "/bin/sh",
        "-c",
        "cat /warmd.json && cd /gpu-operator/tests/pytests && \
         /gpu-operator/ci-internal/run_sanity.sh --type compat --testbed /warmd.json \
         --amdgpu-driver deviceconfig --image-manifest image-manifest/release_v1.2.1_internal_images.yaml"
    ]
    owners: ["email:srivatsa.sangli@amd.com"]
    area:
    sub-area:
    resources:
    - generic:
      - type: K8-GPU-Node
        name: AMD-GPU-MI210
        vendor: Pensando
        count: 1

  gpu-operator-pytest-v1.2.2:
    commands: [
        "/bin/sh",
        "-c",
        "cat /warmd.json && cd /gpu-operator/tests/pytests && \
         /gpu-operator/ci-internal/run_sanity.sh --type compat --testbed /warmd.json \
         --amdgpu-driver deviceconfig --image-manifest image-manifest/release_v1.2.2_internal_images.yaml"
    ]
    owners: ["email:srivatsa.sangli@amd.com"]
    area:
    sub-area:
    resources:
    - generic:
      - type: K8-GPU-Node
        name: AMD-GPU-MI210
        vendor: Pensando
        count: 1

  gpu-operator-pytest-v1.3.0:
    commands: [
        "/bin/sh",
        "-c",
        "cat /warmd.json && cd /gpu-operator/tests/pytests && \
         /gpu-operator/ci-internal/run_sanity.sh --type compat --testbed /warmd.json \
         --amdgpu-driver deviceconfig --image-manifest image-manifest/release_v1.3.0_internal_images.yaml"
    ]
    owners: ["email:srivatsa.sangli@amd.com"]
    area:
    sub-area:
    resources:
    - generic:
      - type: K8-GPU-Node
        name: AMD-GPU-MI210
        vendor: Pensando
        count: 1

  gpu-operator-pytest-v1.3.1:
    commands: [
        "/bin/sh",
        "-c",
        "cat /warmd.json && cd /gpu-operator/tests/pytests && \
         /gpu-operator/ci-internal/run_sanity.sh --type compat --testbed /warmd.json \
         --amdgpu-driver deviceconfig --image-manifest image-manifest/release_v1.3.1_internal_images.yaml"
    ]
    owners: ["email:srivatsa.sangli@amd.com"]
    area:
    sub-area:
    resources:
    - generic:
      - type: K8-GPU-Node
        name: AMD-GPU-MI210
        vendor: Pensando
        count: 1

logfiles:
  - /gpu-operator/tests/pytests/pytest_logs.tgz
